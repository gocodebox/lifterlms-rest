allOf:
  - $ref: '#/components/schemas/Error'
  - properties:
      code:
        default: llms_rest_bad_request
        type: string
      message:
        default: Bad request.
      data:
        properties:
          status:
            default: 400
